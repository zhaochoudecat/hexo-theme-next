# Next 主题修改记录 - 2026-05-03

## 主题配置修改 (`_config.next.yml`)

### 菜单
```yaml
menu:
  tags: /tags/ || fa fa-tags          # 启用标签页
  categories: /categories/ || fa fa-th # 启用分类页
```

### 头像
```yaml
avatar:
  url: /images/saber.jpg  # 设置头像图片
  rounded: true           # 圆形头像
```

### 侧边栏
```yaml
sidebar:
  display: always  # 所有页面展开侧边栏（原 post）
```

## Hexo 配置修改 (`_config.yml`)

### 主题切换
```yaml
theme: next  # 原 cactus-light
```

### 排序方式
```yaml
order_by: title  # 按标题排序（原 -date）
```

## 新增页面

- `source/categories/index.md` — 分类页面
- `source/tags/index.md` — 标签页面
- `source/images/saber.jpg` — 头像图片

## Git
- Remote 改为 `https://github.com/zhaochoudecat/hexo-theme-next`
